@model ARID.Models.SubmissionReviewer
@{
    ViewData["Title"] = "Details";
}
@using System.Globalization
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@inject UserManager<ApplicationUser> UserManager
@{ ApplicationUser currentUser = await UserManager.GetUserAsync(User); }

<h2>الإطلاع على معلومات المراجعة</h2>
<div class="col-lg-12">
    <div class="col-lg-8">
        <hr />
        <dl>
            <dt>
                @Html.DisplayNameFor(model => model.EICInstructions)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.EICInstructions)
            </dd>
            <hr />
            <dt>
                @Html.DisplayNameFor(model => model.DueDate)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.DueDate)
            </dd>
            <hr /><hr />
            <dt>
                @Html.DisplayNameFor(model => model.DateOfRecord)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.DateOfRecord)
            </dd>
            <hr />
            <dt>
                @Html.DisplayNameFor(model => model.ReviewerDecisionToReview)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ReviewerDecisionToReview)
            </dd>
        </dl>
        <div>
        </div>
    </div>
    <div class="col-lg-4">
        <h2></h2>
        <a onclick="javascript:return confirm('هذا الاجراء لايمكن التراجع عنه ، هل تود الاستمرار بهذه العملية؟');" asp-action="ReviewerAgree" asp-route-id="@Model.Id" class="btn btn-success bold col-lg-12">الموافقة على تحكيم البحث</a>
        <br /><br /><br /><br />
        <a onclick="javascript:return confirm('هذا الاجراء لايمكن التراجع عنه ، هل تود الاستمرار بهذه العملية؟');" id="reject"  asp-action="ReviewerReject" asp-route-id="@Model.Id" class="btn btn-default bold col-lg-12">رفض تحكيم البحث</a>
    </div>
</div>
<div>
    <a asp-action="ReviewerReject" asp-route-id="@Model.Id" class="btn btn-primary bold">رجوع</a>
</div>
<style>
    label, h2, input[type='submit'], .bold {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        font-weight: bold;
    }
</style>
@section scripts{
    @*<script>
        $(document).ready(function () {
            $('#agree').click(

                function () {
                       var FollowOptions = {};
                    FollowOptions.url = "/@CultureInfo.CurrentCulture.Name/SubmissionReviewers/ReviewerAgree/";
                    //FollowOptions.url = "/ar-LY/Posts/Follow/";
                    FollowOptions.data = { id: @Model.Id };
                    FollowOptions.success = function () {
                        alert("sucess");
                    };
                   $.ajax(FollowOptions);
                  });
        });
    </script>*@
    @*<script>
        $(document).ready(function () {
            $('#reject').click(

                function () {
                       var FollowOptions = {};
                    FollowOptions.url = "/@CultureInfo.CurrentCulture.Name/SubmissionReviewers/ReviewerReject/";
                    //FollowOptions.url = "/ar-LY/Posts/Follow/";
                    FollowOptions.data = { id: @Model.Id };
                    FollowOptions.success = function () {
                        alert("sucess");
                    };
                   $.ajax(FollowOptions);
                  });
        });
    </script>*@
}